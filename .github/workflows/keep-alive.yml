name: Keep Alive

on:
  schedule:
    - cron: '*/14 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render server
        run: curl -sS --max-time 60 "https://movilbro-crm.onrender.com/health" && echo " OK" || echo " FAIL"