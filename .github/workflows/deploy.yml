name: Deploy to Render

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render deploy
        run: |
          curl -s -X POST "https://api.render.com/deploy/srv-d87dr3mq1p3s73b3a680?key=5k-d_2_3YAs" -w "\nHTTP %{http_code}\n"
